README – Gemini PDF Vector Store Example
This script uses Google Gemini (via MakerSuite API) and LangChain to:

Ask a direct question to Gemini

Load a PDF

Convert its content into vector embeddings

Store them using FAISS for future retrieval

### Install Streamlit

```bash
python -m venv venv
venv\Scripts\activate          
pip install streamlit

streamlit run app.py

###  Install Packages

Open a terminal in the project folder and run:

```bash
pip install google-generativeai
pip install python-dotenv
pip install langchain
pip install langchain-community
pip install langchain-google-genai
pip install faiss-cpu
pip install pypdf
pip install -U langchain-google-genai




